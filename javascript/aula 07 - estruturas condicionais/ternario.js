// Operador ternário

let adm = true;

// condição ? valor_verdadeiro : valor_falso
let label = (adm) ? 'administrador' : 'comum';

if(adm){
    label = 'administrador';
}

alert(`bárbara é um ${label}`)

